# OpenEnv core — use base install to avoid heavy gradio/numpy/pandas deps
# The [core] extras pull in gradio (~400MB) which exceeds 8GB RAM budget
openenv-core>=0.2.1

# Pydantic
pydantic>=2.0,<3.0

# Server
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.9
websockets>=13.0

# HTTP client (used by EnvClient)
httpx>=0.27

# LLM inference
openai>=1.0,<3.0
